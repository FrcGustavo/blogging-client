import { styled } from '@mui/material/styles';
import Box from '@mui/material/Box';

export const CSSBlogApp = styled(Box)({
  width: '100%',
  height: '100%',
  overflow: 'hidden',
  overflowY: 'auto',
});
